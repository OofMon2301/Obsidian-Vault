The gulf of execution is the gap between the **user’s intention (goal)** and the **actions required by the system** to achieve that goal.

For example, if a user wants to save a file but cannot find the “Save” option or the icon is unclear, the gulf of execution is large — the system’s actions don’t match the user’s mental model.