The gulf of evaluation is the gap between the **system’s output/state** and the **user’s ability to interpret it correctly**.

For example, if a user clicks “Save” but receives no confirmation message, they cannot tell whether the file actually saved — the gulf of evaluation is large.